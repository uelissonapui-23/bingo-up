import {describe,it,expect} from 'vitest';import fs from 'node:fs'
const router=fs.readFileSync('src/app/router/AppRouter.tsx','utf8');const migration=fs.readFileSync('supabase/migrations/20260812223000_stage15_multi_role_access_centers.sql','utf8')
describe('centrais multi-papel',()=>{it('separa as rotas operacionais da licença de organizador',()=>{for(const route of ['/acessos','/venda','/operador','/cliente'])expect(router).toContain(route);expect(router.indexOf('path="/venda"')).toBeLessThan(router.indexOf('<Route element={<RequirePlatformAccess/>}>'))});it('permite papéis operacionais independentes no mesmo workspace',()=>{expect(migration).toContain('workspace_operational_memberships');expect(migration).toContain("role in ('seller','draw_operator')");expect(migration).not.toContain('this account already has another role in this workspace')});it('comprador só recebe compras pelo próprio e-mail autenticado',()=>{expect(migration).toContain("select lower(email) into my_email from auth.users where id=auth.uid()");expect(migration).toContain("lower(coalesce(s.buyer_email,''))=my_email")})})
