import type { ButtonHTMLAttributes, PropsWithChildren } from 'react'
import { clsx } from 'clsx'
type Props = PropsWithChildren<ButtonHTMLAttributes<HTMLButtonElement>> & { variant?: 'primary' | 'secondary' | 'danger' }
export function Button({ variant = 'primary', className, children, ...props }: Props) {return <button className={clsx('inline-flex min-h-11 items-center justify-center rounded-xl px-4 py-2 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-50',variant==='primary'&&'bg-red-600 text-white shadow-[0_8px_24px_rgba(220,38,38,.18)] hover:bg-red-500',variant==='secondary'&&'border border-slate-600/60 bg-slate-800/70 text-slate-100 hover:border-slate-500 hover:bg-slate-700/80',variant==='danger'&&'bg-red-700 text-white hover:bg-red-600',className)} {...props}>{children}</button>}
