import {useEffect,useState} from 'react'
import {Link} from 'react-router-dom'
import {Button} from '@/components/ui/Button';import {Card} from '@/components/ui/Card';import {listMyAccessCenters,type AccessEvent} from './accessService'
export function SellerCenterPage(){const [items,setItems]=useState<AccessEvent[]>([]);const [error,setError]=useState<string|null>(null);useEffect(()=>{void listMyAccessCenters().then(v=>setItems(v.seller_events)).catch(e=>setError(e instanceof Error?e.message:'Falha ao carregar.'))},[]);return <RoleShell title="Central do vendedor" subtitle="Você só vê os eventos em que recebeu permissão para vender." error={error}>{items.length?<div className="grid gap-4 md:grid-cols-2">{items.map(i=><Card key={i.event_id}><p className="text-xs font-bold uppercase text-slate-500">{i.workspace_name}</p><h2 className="mt-1 text-xl font-black text-white">{i.event_name}</h2><p className="mt-2 text-sm text-slate-400">{i.starts_at?new Date(i.starts_at).toLocaleString('pt-BR'):'Data não informada'}</p><Link to={`/venda/${i.event_id}`}><Button className="mt-4 w-full">Abrir vendas</Button></Link></Card>)}</div>:<Card>Nenhum evento disponível para venda.</Card>}</RoleShell>}
export function RoleShell({title,subtitle,error,children}:{title:string;subtitle:string;error:string|null;children:React.ReactNode}){return <main className="bingoup-app min-h-dvh p-4 sm:p-6"><div className="mx-auto max-w-6xl space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-sm font-black uppercase tracking-[.16em] text-red-400">BINGOUP</p><h1 className="mt-1 text-3xl font-black text-white">{title}</h1><p className="mt-2 text-sm text-slate-400">{subtitle}</p></div><Link to="/acessos"><Button variant="secondary">Trocar área</Button></Link></div>{error&&<div className="rounded-xl border border-red-900 bg-red-950/30 p-3 text-red-300">{error}</div>}{children}</div></main>}
